-- update 13-08-2025

-- tambah field kd_jenis di tbl_kategori_uangs
ALTER TABLE tbl_kategori_uangs
ADD COLUMN kd_jenis int;

-- update kd_jenis di table tbl_kategori_uangs
-- berdasarkan kd_group di tbl_group_kategoris
UPDATE tbl_kategori_uangs 
INNER JOIN tbl_group_kategoris
ON tbl_kategori_uangs.kd_group = tbl_group_kategoris.kd_group
SET tbl_kategori_uangs.kd_jenis = tbl_group_kategoris.kd_jenis;


-- tambah field kd_kategori di tbl_trans_uang_keluar_act_details
ALTER TABLE tbl_trans_uang_keluar_act_details
ADD COLUMN kd_kategori int;


-- update kd_kategori di table tbl_trans_uang_keluar_act_details
-- berdasarkan kd_trans_keluar di tbl_trans_uang_keluar_act_headers
UPDATE tbl_trans_uang_keluar_act_details 
INNER JOIN tbl_trans_uang_keluar_act_headers
ON tbl_trans_uang_keluar_act_details.kd_trans_keluar = tbl_trans_uang_keluar_act_headers.kd_trans_keluar
SET tbl_trans_uang_keluar_act_details.kd_kategori = tbl_trans_uang_keluar_act_headers.kd_kategori;

-- Hapus kolom kd_kategori di table tbl_trans_uang_keluar_act_headers
ALTER TABLE tbl_trans_uang_keluar_act_headers
DROP COLUMN kd_kategori;

//============
drop view vw_report_act ;

create view vw_report_act as 
SELECT
  `c`.`nm_group` AS `nm_group`,
  `d`.`nm_kategori` AS `nm_kategori`,
  `a`.`no_document` AS `no_document`,
  `a`.`tgl_document` AS `tgl_document`,
  `a`.`total_biaya` AS `total_biaya`,
  `a`.`total_bayar` AS `total_bayar`,
  `a`.`sisa_biaya` AS `sisa_biaya`,
  `a`.`keterangan` AS `keterangan`,
  `e`.`nm_group` AS `pos_uang_masuk`,
  `b`.`tgl_bayar` AS `tgl_bayar`,
  `b`.`jml_bayar` AS `jml_bayar`,
  `b`.`keterangan` AS `keterangan_detail`,
  `a`.`kd_trans_keluar` AS `kd_trans_keluar`,
  `g`.`kd_pembayaran` AS `kd_pembayaran`,
  `g`.`tipe_pembayaran` AS `tipe_pembayaran`,
  `b`.`kd_trans_keluar_detail` AS `kd_trans_keluar_detail`,
  `b`.`kd_kategori` AS `kd_kategori`,
  `b`.`kd_post_uang_masuk` AS `kd_post_uang_masuk` 
FROM
  (
    (
      (
        (
          (`tbl_trans_uang_keluar_act_headers` `a` JOIN `tbl_trans_uang_keluar_act_details` `b` ON (`a`.`kd_trans_keluar` = `b`.`kd_trans_keluar`))
        JOIN `tbl_group_kategoris` `c` ON (`a`.`kd_group` = `c`.`kd_group`))
      JOIN `tbl_kategori_uangs` `d` ON (`b`.`kd_kategori` = `d`.`kd_kategori`))
    LEFT JOIN `tbl_group_kategoris` `e` ON (`b`.`kd_post_uang_masuk` = `e`.`kd_group`))
  LEFT JOIN `tbl_tipe_pembayarans` `g` ON (`b`.`kd_pembayaran` = `g`.`kd_pembayaran`)) 
WHERE
  `a`.`flag_aktif` = 0 
  AND `b`.`flag_aktif` = 0 
  AND `c`.`flag_aktif` = 0 
  AND `d`.`flag_aktif` = 0 
ORDER BY
  `a`.`tgl_document`,
  `a`.`no_document`,
  `b`.`seqno` ;

  //===================

daftar nomor api direvisi
4
18


//==============================================================================================

-- update 14-08-2025

-- tambah field kd_kategori di tbl_trans_uang_masuk_lain_details
ALTER TABLE tbl_trans_uang_masuk_lain_details
ADD COLUMN kd_kategori int;


-- update kd_kategori di table tbl_trans_uang_masuk_lain_details
-- berdasarkan kd_trans_masuk_lain di tbl_trans_uang_masuk_lain_headers
UPDATE tbl_trans_uang_masuk_lain_details 
INNER JOIN tbl_trans_uang_masuk_lain_headers
ON tbl_trans_uang_masuk_lain_details.kd_trans_masuk_lain = tbl_trans_uang_masuk_lain_headers.kd_trans_masuk_lain
SET tbl_trans_uang_masuk_lain_details.kd_kategori = tbl_trans_uang_masuk_lain_headers.kd_kategori;

-- Hapus kolom kd_kategori di table tbl_trans_uang_masuk_lain_headers
ALTER TABLE tbl_trans_uang_masuk_lain_headers
DROP COLUMN kd_kategori;

daftar nomor api direvisi
15


//===============================
api no 4
tambahan List Data New untuk keluarin jenis keluar/jenis masuk 



update 04-09-2025

create TABLE test.tbl_one_group_kategori as 
SELECT kd_group, COUNT(kd_kategori) AS jml_kategori
FROM smaalkha_db_bendahara.tbl_kategori_uangs
GROUP BY kd_group
HAVING COUNT(kd_kategori) = 1
ORDER BY jml_kategori ASC, kd_group;

create TABLE test.tbl_moreone_group_kategori as 
SELECT kd_group, COUNT(kd_kategori) AS jml_kategori
FROM smaalkha_db_bendahara.tbl_kategori_uangs
GROUP BY kd_group
HAVING COUNT(kd_kategori) > 1
ORDER BY jml_kategori ASC, kd_group;


UPDATE smaalkha_db_bendahara.tbl_trans_uang_keluar_act_details d
JOIN smaalkha_db_bendahara.tbl_kategori_uangs k 
  ON d.kd_post_uang_masuk = k.kd_group
SET d.kd_post_uang_masuk = k.kd_kategori
WHERE k.kd_group IN (
    SELECT kd_group 
    FROM test.tbl_one_group_kategori
);

UPDATE smaalkha_db_bendahara.tbl_trans_uang_keluar_pra_act_details d
JOIN smaalkha_db_bendahara.tbl_kategori_uangs k 
  ON d.kd_post_uang_masuk = k.kd_group
SET d.kd_post_uang_masuk = k.kd_kategori
WHERE k.kd_group IN (
    SELECT kd_group 
    FROM test.tbl_one_group_kategori
);


UPDATE smaalkha_db_bendahara.tbl_trans_uang_keluar_act_details d
JOIN (
    SELECT k.kd_group, MIN(k.kd_kategori) AS kd_kategori
    FROM smaalkha_db_bendahara.tbl_kategori_uangs k
    WHERE k.kd_group IN (
        SELECT kd_group 
        FROM test.tbl_moreone_group_kategori
    )
    GROUP BY k.kd_group
) m ON d.kd_post_uang_masuk = m.kd_group
SET d.kd_post_uang_masuk = m.kd_kategori;


UPDATE smaalkha_db_bendahara.tbl_trans_uang_keluar_pra_act_details d
JOIN (
    SELECT k.kd_group, MIN(k.kd_kategori) AS kd_kategori
    FROM smaalkha_db_bendahara.tbl_kategori_uangs k
    WHERE k.kd_group IN (
        SELECT kd_group 
        FROM test.tbl_moreone_group_kategori
    )
    GROUP BY k.kd_group
) m ON d.kd_post_uang_masuk = m.kd_group
SET d.kd_post_uang_masuk = m.kd_kategori;


drop view vw_s_masuk ;
create view vw_s_masuk as 
SELECT
  `vw_report_ppdb`.`nm_group` AS `nm_group`,
  `vw_report_ppdb`.`nm_kategori` AS `nm_kategori`,
  `vw_report_ppdb`.`tgl_bayar` AS `tgl_bayar`,
  `vw_report_ppdb`.`total_bayar` AS `total_bayar`,
  `vw_report_ppdb`.`kd_kategori` AS `kd_kategori`,
  `vw_report_ppdb`.`jml_bayar` AS `jml_bayar`,
  `vw_report_ppdb`.`tipe_pembayaran` AS `tipe_pembayaran`,
  `vw_report_ppdb`.`kd_group` AS `kd_group`,
  `vw_report_ppdb`.`nik` AS `nik`,
  `vw_report_ppdb`.`nm_siswa` AS `nm_siswa`,
  `vw_report_ppdb`.`kategori_biaya_ppdb` AS `keterangan` 
FROM
  `vw_report_ppdb` 
WHERE
  `vw_report_ppdb`.`tgl_bayar` IS NOT NULL 
  AND `vw_report_ppdb`.`jml_bayar` IS NOT NULL 
GROUP BY
  `vw_report_ppdb`.`kd_trans_masuk_detail_ppdb` UNION ALL
SELECT
  `vw_report_spp`.`nm_group` AS `nm_group`,
  `vw_report_spp`.`nm_kategori` AS `nm_kategori`,
  `vw_report_spp`.`tgl_bayar` AS `tgl_bayar`,
  `vw_report_spp`.`total_bayar` AS `total_bayar`,
  `vw_report_spp`.`kd_kategori` AS `kd_kategori`,
  `vw_report_spp`.`jml_bayar` AS `jml_bayar`,
  `vw_report_spp`.`tipe_pembayaran` AS `tipe_pembayaran`,
  `vw_report_spp`.`kd_group` AS `kd_group`,
  `vw_report_spp`.`nis_siswa` AS `nis_siswa`,
  `vw_report_spp`.`nm_siswa` AS `nm_siswa` ,
   `vw_report_spp`.`keterangan_detail` AS `keterangan` 
FROM
  `vw_report_spp` 
WHERE
  `vw_report_spp`.`tgl_bayar` IS NOT NULL 
  AND `vw_report_spp`.`jml_bayar` IS NOT NULL 
GROUP BY
  `vw_report_spp`.`kd_trans_masuk_detail` UNION ALL
SELECT
  `vw_report_umsiswa`.`nm_group` AS `nm_group`,
  `vw_report_umsiswa`.`nm_kategori` AS `nm_kategori`,
  `vw_report_umsiswa`.`tgl_bayar` AS `tgl_bayar`,
  `vw_report_umsiswa`.`total_bayar` AS `total_bayar`,
  `vw_report_umsiswa`.`kd_kategori` AS `kd_kategori`,
  `vw_report_umsiswa`.`jml_bayar` AS `jml_bayar`,
  `vw_report_umsiswa`.`tipe_pembayaran` AS `tipe_pembayaran`,
  `vw_report_umsiswa`.`kd_group` AS `kd_group`,
  `vw_report_umsiswa`.`nis_siswa` AS `nis_siswa`,
  `vw_report_umsiswa`.`nm_siswa` AS `nm_siswa` ,
   `vw_report_umsiswa`.`keterangan_detail` AS `keterangan`
FROM
  `vw_report_umsiswa` 
WHERE
  `vw_report_umsiswa`.`tgl_bayar` IS NOT NULL 
  AND `vw_report_umsiswa`.`jml_bayar` IS NOT NULL 
GROUP BY
  `vw_report_umsiswa`.`kd_trans_masuk_detail_siswa` UNION ALL
SELECT
  `vw_report_umlain`.`nm_group` AS `nm_group`,
  `vw_report_umlain`.`nm_kategori` AS `nm_kategori`,
  `vw_report_umlain`.`tgl_bayar` AS `tgl_bayar`,
  `vw_report_umlain`.`total_bayar` AS `total_bayar`,
  `vw_report_umlain`.`kd_kategori` AS `kd_kategori`,
  `vw_report_umlain`.`jml_bayar` AS `jml_bayar`,
  `vw_report_umlain`.`tipe_pembayaran` AS `tipe_pembayaran`,
  `vw_report_umlain`.`kd_group` AS `kd_group`,
  `vw_report_umlain`.`no_document` AS `no_document`,
  `vw_report_umlain`.`keterangan` AS `keterangan_header`,
  `vw_report_umlain`.`keterangan_detail` AS `keterangan`  
FROM
  `vw_report_umlain` 
WHERE
  `vw_report_umlain`.`tgl_bayar` IS NOT NULL 
  AND `vw_report_umlain`.`jml_bayar` IS NOT NULL 
GROUP BY
  `vw_report_umlain`.`kd_trans_masuk_detail_lain` ;


drop view vw_s_keluar ;
create view vw_s_keluar as 
SELECT
	`vw_report_act`.`nm_group` AS `nm_group`,
	`vw_report_act`.`nm_kategori` AS `nm_kategori`,
	`vw_report_act`.`tgl_bayar` AS `tgl_bayar`,
	`vw_report_act`.`total_bayar` AS `total_bayar`,
	`vw_report_act`.`kd_kategori` AS `kd_kategori`,
	`vw_report_act`.`jml_bayar` AS `jml_bayar`,
	`vw_report_act`.`tipe_pembayaran` AS `tipe_pembayaran`,
	`vw_report_act`.`pos_uang_masuk` AS `pos_uang_masuk`,
	`vw_report_act`.`kd_post_uang_masuk` AS `kd_post_uang_masuk`,
	`vw_report_act`.`no_document` AS `no_document`,
	`vw_report_act`.`tgl_document` AS `tgl_document`,
  `vw_report_act`.`keterangan_detail` AS `keterangan`  
FROM
	`vw_report_act` 
WHERE
	`vw_report_act`.`tgl_bayar` IS NOT NULL 
	AND `vw_report_act`.`jml_bayar` IS NOT NULL UNION ALL
SELECT
	`vw_report_pra_act`.`nm_group` AS `nm_group`,
	`vw_report_pra_act`.`nm_kategori` AS `nm_kategori`,
	`vw_report_pra_act`.`tgl_bayar` AS `tgl_bayar`,
	`vw_report_pra_act`.`total_bayar` AS `total_bayar`,
	`vw_report_pra_act`.`kd_kategori` AS `kd_kategori`,
	`vw_report_pra_act`.`jml_bayar` AS `jml_bayar`,
	`vw_report_pra_act`.`tipe_pembayaran` AS `tipe_pembayaran`,
	`vw_report_pra_act`.`pos_uang_masuk` AS `pos_uang_masuk`,
	`vw_report_pra_act`.`kd_post_uang_masuk` AS `kd_post_uang_masuk`,
	`vw_report_pra_act`.`no_document` AS `no_document`,
	`vw_report_pra_act`.`tgl_document` AS `tgl_document` ,
  `vw_report_pra_act`.`keterangan_detail` AS `keterangan`  
FROM
	`vw_report_pra_act` 
WHERE
	`vw_report_pra_act`.`tgl_bayar` IS NOT NULL 
	AND `vw_report_pra_act`.`jml_bayar` IS NOT NULL ;



//perbaikan pos_uang_masuk kosong ""
drop view vw_report_act;
CREATE view vw_report_act as 
SELECT
  `c`.`nm_group` AS `nm_group`,
  `d`.`nm_kategori` AS `nm_kategori`,
  `a`.`no_document` AS `no_document`,
  `a`.`tgl_document` AS `tgl_document`,
  `a`.`total_biaya` AS `total_biaya`,
  `a`.`total_bayar` AS `total_bayar`,
  `a`.`sisa_biaya` AS `sisa_biaya`,
  `a`.`keterangan` AS `keterangan`,
  `e`.`nm_kategori` AS `pos_uang_masuk`,
  `b`.`tgl_bayar` AS `tgl_bayar`,
  `b`.`jml_bayar` AS `jml_bayar`,
  `b`.`keterangan` AS `keterangan_detail`,
  `a`.`kd_trans_keluar` AS `kd_trans_keluar`,
  `g`.`kd_pembayaran` AS `kd_pembayaran`,
  `g`.`tipe_pembayaran` AS `tipe_pembayaran`,
  `b`.`kd_trans_keluar_detail` AS `kd_trans_keluar_detail`,
  `b`.`kd_kategori` AS `kd_kategori`,
  `b`.`kd_post_uang_masuk` AS `kd_post_uang_masuk` 
FROM
  (
    (
      (
        (
          (`tbl_trans_uang_keluar_act_headers` `a` JOIN `tbl_trans_uang_keluar_act_details` `b` ON (`a`.`kd_trans_keluar` = `b`.`kd_trans_keluar`))
        JOIN `tbl_group_kategoris` `c` ON (`a`.`kd_group` = `c`.`kd_group`))
      JOIN `tbl_kategori_uangs` `d` ON (`b`.`kd_kategori` = `d`.`kd_kategori`))
    LEFT JOIN `tbl_kategori_uangs` `e` ON (`b`.`kd_post_uang_masuk` = `e`.`kd_kategori`))
  LEFT JOIN `tbl_tipe_pembayarans` `g` ON (`b`.`kd_pembayaran` = `g`.`kd_pembayaran`)) 
WHERE
  `a`.`flag_aktif` = 0 
  AND `b`.`flag_aktif` = 0 
  AND `c`.`flag_aktif` = 0 
  AND `d`.`flag_aktif` = 0 
ORDER BY
  `a`.`tgl_document`,
  `a`.`no_document`,
  `b`.`seqno`  ;




jadi kemarin yg bikin lama w mikirin ini
ada peralihan kd_group ke kd_kategori
yang lama soalnya w simpannya kd_group bukan kd_kategori
di field kd_post_uang_masuk , 
jadi w ngakalin buat update semua kd_post_uang_masuk yg save kd_group
jadi ke kd_kategori , makanya w buat dua table test buat patokan misahin untuk update data

1. table tbl_one_group_kategori adalah kd_group yang mempunyai 1 kd_group = 1 kd_kategori
2. table tbl_moreone_group_kategori adalah kd_group yang mempunai 1 kd_group > 1 kd_kategori

contoh 1
kd_group kd_kategori
1			1

contoh 2
kd_group kd_kategori
4			8
4			12
4			313
4			314
4			373

untuk point 1 gampang penanganannya karena tinggal pakai kd_kategori 1
untuk point 2 w update dengan yang kd_kategori yang paling atas 8

kalau dari table yang w cek kd_post_uang_masuk = 4 itu gak ada
kd_kategori 4 itu tagihan air uang keluar .

http://127.0.0.1:2022/api/v1/transaksi/uangkeluaract/listdata
udah w update nm_group didetail itu sebenernya nm_kategori ,
kalo namain nm_kategori double soalnya , ada kd_post_uang_masuk dan kd_kategori

http://127.0.0.1:2022/api/v1/report/uangkeluar/reportact
udah w update nm_kategori di header w udah pindahin kedetail










































